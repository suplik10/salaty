<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">

    <title>{ifset title}{include title|stripHtml} | {/ifset}Saláty</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700" rel="stylesheet">
    <link rel="stylesheet" href="{$basePath}/css/main.css?{time()}">
    <link rel="stylesheet" href="{$basePath}/css/lightslider.min.css?{time()}">
</head>

<body data-spy="scroll" data-target="#categories" data-offset="75">
<nav class="navbar navbar-light navbar-expand-lg fixed-top justify-content-end" id="top-menu">
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link active" href="{plink Homepage:offers}">NABÍDKA</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{plink Homepage:default}">KONTAKTY</a>
        </li>
    </ul>
</nav>
<div n:foreach="$flashes as $flash" n:class="flash, $flash->type">{$flash->message}</div>
<div id="content">
    {include content}
</div>

{block scripts}
    <script src="{$basePath}/js/main.js"></script>
{/block}
</body>
</html>
