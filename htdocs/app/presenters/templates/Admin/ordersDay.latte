{block content}
    <div class="container-fluid pt15" id="administrace">
        <div class="row">
            <div class="col-sm-3 col-md-2">
                {include "left-menu.latte"}
            </div>
            <div class="col-sm-9 col-md-10">
                <div class="tab-content" id="admin-menuContent">
                    <h1>Administrace</h1>

                    {* OBJEDNÁVKY *}
                    <div id="orders-day">
                        {snippet orders-day}
                        {form changeDateForm}
                            <div class="form-group">
                                <div class="row">
                                    <div class="input-group col-xl-6">
                                        <div class="input-group-addon">
                                            {label date}
                                        </div>
                                        {input date, class=>'form-control'}
                                        <div class="input-group-addon">
                                            {input send}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {/form}
                            <h2>Objednávky na {$date->format('d. m. Y')}</h2>
                            <table class="table">
                                <thead class="thead-dark">
                                <tr>
                                    <th>Produkt</th>
                                    <th>Množství</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr n:foreach="$products as $product">
                                    <th>{$product->name}</th>
                                    <th>x{$product->quantity}</th>
                                </tr>
                                <tr n:if="empty($products)">
                                    <td>Na tento datum nejsou objednány žádné produkty.</td>
                                </tr>
                                </tbody>
                            </table>
                            <table class="table">
                                <thead class="thead-dark">
                                <tr>
                                    <th>Ingredience</th>
                                    <th>Váha (v gramech)</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr n:foreach="$ingredients as $ingredient">
                                    <th>{$ingredient->name}</th>
                                    <th>{$ingredient->total_weight}g</th>
                                </tr>
                                <tr n:if="empty($ingredients)">
                                    <td>Na tento datum nejsou objednány žádné produkty.</td>
                                </tr>
                                </tbody>
                            </table>
                        {/snippet}
                    </div>
                </div>
            </div>
        </div>
    </div>
{/block}